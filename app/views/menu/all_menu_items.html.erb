<!DOCTYPE html>
<html>
<head>
	<title></title>
</head>
<body>
	<h1 class="text-white">All Menu Items</h1>
		<div class="row">
			<div class="card-deck">
			  <% @menu_items.each do |item| %>
				    <div class="col-md-4 d-flex align-items-stretch">
				      <div class="card mb-4 border-light">
				      	<div class="card-header text-white bg-danger">
				      		<h3><%= item.name %></h3>
				      	</div>

				        <div class="card-body bg-light">
				        	<h5><%= number_to_currency item.price %></h5>
				        	<p><%= item.description %></p>
				        	<button type="button" class="btn btn-info btn-sm" data-toggle="modal" data-target="#myModal">
				        		Add to Catering Order
							</button>
				
							<div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
							  <div class="modal-dialog" role="document">
							    <div class="modal-content">
							      <div class="modal-header">
							        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">×</span></button>
							        <h4 class="modal-title" id="myModalLabel">Add <%= item.name %> to Order</h4>
							      </div>
							      <div class="modal-body">
								    <%= form_with(url: add_to_cart_path) do |form| %>
					            		<%= form.hidden_field :menu_item_id, value: item.id %>
					            		<%= form.number_field :quantity, value: 1 %>
					            		<%= form.submit "Add to Catering Order", class: "btn btn-info" %>
				            		<% end %>

							      </div>
							     
							    </div>
							  </div>
							</div>
				        </div>

				      </div>
				    </div>
			  <% end %>
			</div>
		</div>



</body>
</html>